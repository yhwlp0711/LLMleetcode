// 常见 NumPy / PyTorch API 的简明说明，用于题面/解析里 inline code 的悬浮提示。
// key 用 API 的规范写法（去掉参数），匹配时会做归一化（见 apiTooltip.tsx）。

export interface ApiDoc {
  sig: string // 简化签名
  desc: string // 一句话作用
  op: string // 具体做了什么操作
  inputs: string // 输入
  outputs: string // 输出
}

export const API_DOCS: Record<string, ApiDoc> = {
  // ---- NumPy ----
  'np.where': {
    sig: 'np.where(cond, a, b)',
    desc: '向量化的三元运算符：逐元素在 a、b 之间选一个。',
    op: '遍历每个位置，若该位置 cond 为 True 就取 a 的对应元素，否则取 b 的对应元素，等价于 `a if cond else b` 的逐元素版本。',
    inputs: 'cond 布尔数组；a、b 与 cond 可广播。',
    outputs: 'cond 为 True 处取 a，否则取 b，形状同广播结果。',
  },
  'np.argmax': {
    sig: 'np.argmax(x, axis=None)',
    desc: '返回沿指定轴最大值所在的索引。',
    op: '沿 axis 扫描，找出最大值的位置下标；多个并列时返回第一个（最小索引）。',
    inputs: 'x 数组；axis 指定轴（None 表示展平）。',
    outputs: '最大值的索引（并列时取最小索引）。',
  },
  'np.moveaxis': {
    sig: 'np.moveaxis(x, src, dst)',
    desc: '把某个轴移动到新位置，比 transpose 更易读。',
    op: '把第 src 个轴抽出来插到第 dst 位，其余轴顺序不变；只改变维度排列，不复制数据。',
    inputs: 'x 数组；src/dst 原位置与目标位置。',
    outputs: '轴被重排后的视图（不复制数据）。',
  },
  'np.cumsum': {
    sig: 'np.cumsum(x, axis)',
    desc: '沿轴做累加（前缀和）。',
    op: '沿 axis 从头往后逐个累加：结果第 i 个元素 = 输入前 i 个元素之和。',
    inputs: 'x 数组；axis 累加的轴。',
    outputs: '同形数组，每个位置是该轴上到此为止的累加和。',
  },
  'np.bincount': {
    sig: 'np.bincount(x, weights, minlength)',
    desc: '统计非负整数数组里每个值出现的次数（或加权和）。',
    op: '把每个整数当作桶的下标，累加计数（有 weights 时累加权重），相当于向量化的直方图。',
    inputs: 'x 非负整数一维数组；weights 可选权重。',
    outputs: '长度为 max(x)+1 的计数数组。',
  },
  'np.concatenate': {
    sig: 'np.concatenate([a, b, ...], axis)',
    desc: '沿已有轴把多个数组拼接起来。',
    op: '沿 axis 把多个数组首尾相接，该轴长度相加，其余维度必须一致。',
    inputs: '数组列表；axis 拼接的轴（其余维度须一致）。',
    outputs: '拼接后的数组。',
  },
  'np.einsum': {
    sig: 'np.einsum("ij,jk->ik", a, b)',
    desc: '爱因斯坦求和：用下标记法表达乘加、转置、求和等。',
    op: '按下标字符串：重复出现的下标做乘法并求和，未出现在输出的下标被求和掉，可一式表达矩阵乘/转置/求和。',
    inputs: '下标字符串 + 若干数组。',
    outputs: '按下标规则收缩后的数组。',
  },
  'np.diff': {
    sig: 'np.diff(x, axis)',
    desc: '沿轴求相邻元素的差分。',
    op: '沿 axis 计算 x[i+1] - x[i]，输出比输入在该轴上少一个元素。',
    inputs: 'x 数组；axis 差分的轴。',
    outputs: '该轴长度减 1 的差分数组。',
  },
  'np.linalg.svd': {
    sig: 'np.linalg.svd(A)',
    desc: '奇异值分解 A = U·Σ·Vᵀ。',
    op: '把矩阵分解为两个正交矩阵 U、V 和一组非负奇异值，用于 PCA、降维、求秩等。',
    inputs: 'A 二维矩阵。',
    outputs: 'U、奇异值 s、Vᵀ 三部分。',
  },

  // ---- PyTorch ----
  'torch.where': {
    sig: 'torch.where(cond, a, b)',
    desc: '向量化三元运算符：逐元素在 a、b 之间选。',
    op: '逐元素判断 cond：True 取 a 的对应元素，False 取 b 的对应元素。',
    inputs: 'cond 布尔张量；a、b 可广播。',
    outputs: 'cond 为 True 取 a，否则取 b。',
  },
  'torch.cat': {
    sig: 'torch.cat([a, b, ...], dim)',
    desc: '沿已有维度拼接张量。',
    op: '沿 dim 把多个张量首尾相接，该维长度相加，其余维度须一致（不新增维度）。',
    inputs: '张量列表；dim 拼接的维度。',
    outputs: '拼接后的张量。',
  },
  'torch.stack': {
    sig: 'torch.stack([a, b, ...], dim)',
    desc: '在新维度上堆叠多个同形张量。',
    op: '在 dim 处插入一个新维度，把 N 个同形张量叠成一层，结果比输入多一维。',
    inputs: '同形张量列表；dim 新维度位置。',
    outputs: '比输入多一维的张量。',
  },
  'torch.arange': {
    sig: 'torch.arange(start, end, step)',
    desc: '生成等差整数/浮点序列（不含 end）。',
    op: '从 start 开始每次加 step，直到（不含）end，生成一维等差序列。',
    inputs: 'start、end、step。',
    outputs: '一维张量。',
  },
  'torch.exp': {
    sig: 'torch.exp(x)',
    desc: '逐元素求自然指数 eˣ。',
    op: '对每个元素计算 e 的该值次幂。',
    inputs: 'x 张量。',
    outputs: '同形张量。',
  },
  'torch.log': {
    sig: 'torch.log(x)',
    desc: '逐元素求自然对数 ln(x)。',
    op: '对每个元素取自然对数；输入需为正，否则得到 -inf 或 nan。',
    inputs: 'x 正值张量。',
    outputs: '同形张量。',
  },
  'torch.logsumexp': {
    sig: 'torch.logsumexp(x, dim)',
    desc: '数值稳定地计算 log(Σ eˣ)，softmax/交叉熵的核心。',
    op: '先减去该维最大值再 exp、求和、取 log、加回最大值，避免 exp 溢出，等价于 log(Σ eˣ)。',
    inputs: 'x 张量；dim 归约的维度。',
    outputs: '沿 dim 归约后的张量。',
  },
  'torch.sigmoid': {
    sig: 'torch.sigmoid(x)',
    desc: '逐元素 sigmoid：1/(1+e⁻ˣ)，映射到 (0,1)。',
    op: '对每个元素计算 1/(1+e⁻ˣ)，把任意实数压到 (0,1)，常用作二分类概率。',
    inputs: 'x 张量。',
    outputs: '同形张量，值域 (0,1)。',
  },
  'torch.softmax': {
    sig: 'torch.softmax(x, dim)',
    desc: '沿维度归一化成概率分布（和为 1）。',
    op: '沿 dim 对每个元素求 eˣ 再除以该维的 eˣ 之和，得到非负且和为 1 的概率。',
    inputs: 'x 张量；dim 归一化的维度。',
    outputs: '同形张量，沿 dim 求和为 1。',
  },
  'F.softmax': {
    sig: 'F.softmax(x, dim)',
    desc: '沿维度归一化成概率分布（和为 1）。',
    op: '沿 dim 对每个元素求 eˣ 再除以该维的 eˣ 之和，得到非负且和为 1 的概率。',
    inputs: 'x 张量；dim 归一化的维度。',
    outputs: '同形张量，沿 dim 求和为 1。',
  },
  'F.log_softmax': {
    sig: 'F.log_softmax(x, dim)',
    desc: '数值稳定的 log(softmax(x))。',
    op: '等价于先 softmax 再取 log，但内部用 x - logsumexp(x) 直接算，避免中间概率下溢。',
    inputs: 'x 张量；dim 维度。',
    outputs: '同形张量（对数概率，≤ 0）。',
  },
  'F.logsigmoid': {
    sig: 'F.logsigmoid(x)',
    desc: '数值稳定的 log(sigmoid(x))，DPO 等损失常用。',
    op: '直接算 -softplus(-x) = -log(1+e⁻ˣ)，避免先 sigmoid 再 log 在负值区下溢。',
    inputs: 'x 张量。',
    outputs: '同形张量（≤ 0）。',
  },
  'torch.erf': {
    sig: 'torch.erf(x)',
    desc: '误差函数，精确版 GELU 会用到。',
    op: '逐元素计算高斯误差函数 erf(x)，即标准正态积分的两倍减一，用于精确 GELU。',
    inputs: 'x 张量。',
    outputs: '同形张量，值域 (-1, 1)。',
  },
  'torch.clamp': {
    sig: 'torch.clamp(x, min, max)',
    desc: '把张量元素裁剪到 [min, max] 区间，PPO 裁剪用它。',
    op: '逐元素：小于 min 的置为 min，大于 max 的置为 max，区间内不变。',
    inputs: 'x 张量；min/max 边界。',
    outputs: '同形张量，超界的被截断。',
  },
  'torch.topk': {
    sig: 'torch.topk(x, k, dim)',
    desc: '取沿维度最大的 k 个值及其索引。',
    op: '沿 dim 排序找出最大的 k 个元素，同时返回它们的值和原始位置索引。',
    inputs: 'x 张量；k 个数；dim 维度。',
    outputs: '(values, indices) 两个张量。',
  },
  'torch.multinomial': {
    sig: 'torch.multinomial(p, n)',
    desc: '按概率分布采样索引，top-k/top-p 采样用它。',
    op: '把 p 当作各类别的概率权重，按此分布随机抽 n 个类别下标（有放回或无放回）。',
    inputs: 'p 概率张量（非负）；n 采样个数。',
    outputs: '采样到的索引张量。',
  },
  'torch.tril': {
    sig: 'torch.tril(x)',
    desc: '取矩阵下三角（上三角置 0），构造因果 mask 用。',
    op: '保留主对角线及以下的元素，对角线以上全部置 0；用于让注意力只看到当前及之前的位置。',
    inputs: 'x 二维/批量方阵。',
    outputs: '下三角保留、其余为 0 的张量。',
  },
  'torch.gather': {
    sig: 'torch.gather(x, dim, index)',
    desc: '沿维度按索引取值，交叉熵取目标类 log-prob 用。',
    op: '沿 dim 按 index 给出的下标逐位置取元素，输出形状与 index 相同；常用来取每行目标类的值。',
    inputs: 'x 张量；dim 维度；index 同形索引张量。',
    outputs: '与 index 同形的张量。',
  },
  'torch.no_grad': {
    sig: 'with torch.no_grad():',
    desc: '上下文内关闭梯度追踪，省显存、加速推理。',
    op: '进入该 with 块后，块内所有张量运算都不构建计算图、不记录梯度，退出后恢复。',
    inputs: '无（上下文管理器）。',
    outputs: '块内张量运算不记录计算图。',
  },
  'torch.zeros': {
    sig: 'torch.zeros(*shape)',
    desc: '创建指定形状的全 0 张量。',
    op: '按给定形状分配一块新张量并把所有元素初始化为 0。',
    inputs: 'shape；可选 dtype/device。',
    outputs: '全 0 张量。',
  },
  'torch.ones': {
    sig: 'torch.ones(*shape)',
    desc: '创建指定形状的全 1 张量。',
    op: '按给定形状分配一块新张量并把所有元素初始化为 1。',
    inputs: 'shape；可选 dtype/device。',
    outputs: '全 1 张量。',
  },
  'torch.zeros_like': {
    sig: 'torch.zeros_like(x)',
    desc: '创建和 x 同形状、同 dtype/device 的全 0 张量。',
    op: '照抄 x 的形状与类型，元素全填 0。',
    inputs: 'x 张量。',
    outputs: '与 x 同形的全 0 张量。',
  },
  'torch.empty_like': {
    sig: 'torch.empty_like(x)',
    desc: '创建和 x 同形状的未初始化张量（内容是垃圾值）。',
    op: '只分配内存不清零，需自己写入；比 zeros_like 略快。',
    inputs: 'x 张量。',
    outputs: '与 x 同形、内容未定义的张量。',
  },
  'torch.full_like': {
    sig: 'torch.full_like(x, value)',
    desc: '创建和 x 同形状、全部填 value 的张量。',
    op: '照抄 x 的形状与类型，元素全填成给定的 value。',
    inputs: 'x 张量；value 填充值。',
    outputs: '与 x 同形的常量张量。',
  },
  'torch.matmul': {
    sig: 'torch.matmul(a, b)',
    desc: '矩阵乘法（支持 batch 广播），等价于 a @ b。',
    op: '对最后两维做矩阵乘，前面的 batch 维按广播规则对齐。',
    inputs: 'a、b 张量（内维需匹配）。',
    outputs: '矩阵乘结果。',
  },
  'torch.min': {
    sig: 'torch.min(a, b) / torch.min(x, dim)',
    desc: '逐元素取较小值，或沿维度求最小值。',
    op: '两个张量时逐元素取 min；给 dim 时沿该维归约返回 (values, indices)。',
    inputs: 'a、b 可广播；或 x + dim。',
    outputs: '较小值张量；或 (最小值, 索引)。',
  },
  'torch.sqrt': {
    sig: 'torch.sqrt(x)',
    desc: '逐元素求平方根。',
    op: '对每个元素开平方；负值得到 nan。',
    inputs: 'x 非负张量。',
    outputs: '同形张量。',
  },
  'torch.rsqrt': {
    sig: 'torch.rsqrt(x)',
    desc: '逐元素求平方根的倒数 1/√x，RMSNorm 用它。',
    op: '算 1/sqrt(x)，比先 sqrt 再取倒数更快更稳。',
    inputs: 'x 正值张量。',
    outputs: '同形张量。',
  },
  'torch.tanh': {
    sig: 'torch.tanh(x)',
    desc: '逐元素双曲正切，值域 (-1, 1)，GELU 近似用它。',
    op: '对每个元素算 tanh，把实数压到 (-1, 1)。',
    inputs: 'x 张量。',
    outputs: '同形张量。',
  },
  'F.silu': {
    sig: 'F.silu(x)',
    desc: 'SiLU / Swish 激活：x · sigmoid(x)。',
    op: '逐元素算 x * sigmoid(x)，是 ReLU 的平滑版。',
    inputs: 'x 张量。',
    outputs: '同形张量。',
  },
  'F.gelu': {
    sig: 'F.gelu(x, approximate="none")',
    desc: 'GELU 激活，approximate 选精确（erf）或 tanh 近似。',
    op: '逐元素算 x·Φ(x)（Φ 是标准正态 CDF），BERT/GPT 常用。',
    inputs: 'x 张量；approximate="none"/"tanh"。',
    outputs: '同形张量。',
  },
  'F.glu': {
    sig: 'F.glu(x, dim)',
    desc: '门控线性单元：把张量沿 dim 对半切，前半 × sigmoid(后半)。',
    op: '沿 dim 分成 a、b 两半，输出 a * sigmoid(b)。',
    inputs: 'x 张量（该维为偶数）；dim。',
    outputs: '该维减半的张量。',
  },
  'F.cross_entropy': {
    sig: 'F.cross_entropy(logits, target)',
    desc: '交叉熵损失（内部做 log_softmax + NLL）。',
    op: '对 logits 做稳定 log_softmax，再取目标类的负对数似然并平均。',
    inputs: 'logits (N,C)；target (N,) 类别下标。',
    outputs: '标量损失。',
  },
  'F.nll_loss': {
    sig: 'F.nll_loss(log_probs, target)',
    desc: '负对数似然损失，输入已是 log 概率。',
    op: '取每个样本目标类的 log 概率，取负后平均。',
    inputs: 'log_probs (N,C)；target (N,)。',
    outputs: '标量损失。',
  },
  'F.kl_div': {
    sig: 'F.kl_div(log_q, p, reduction)',
    desc: 'KL 散度损失，注意 input 传的是 log 概率。',
    op: '算 Σ p·(log p − log q)；reduction="batchmean" 才除以 batch。',
    inputs: 'log_q 对数概率；p 概率（target）。',
    outputs: '标量（按 reduction 归约）。',
  },
  'F.binary_cross_entropy_with_logits': {
    sig: 'F.binary_cross_entropy_with_logits(logits, target)',
    desc: '数值稳定的二分类交叉熵，输入是 logits（未过 sigmoid）。',
    op: '内部用稳定公式合并 sigmoid + BCE，避免 log(0)。',
    inputs: 'logits 与 target 同形（target ∈ {0,1}）。',
    outputs: '标量损失。',
  },
  'F.scaled_dot_product_attention': {
    sig: 'F.scaled_dot_product_attention(q, k, v, attn_mask)',
    desc: 'PyTorch 内置的缩放点积注意力（含 FlashAttention 优化）。',
    op: '算 softmax(QKᵀ/√d + mask)·V，一次调用完成整个注意力。',
    inputs: 'q/k/v 张量；可选 attn_mask / is_causal。',
    outputs: '注意力输出张量。',
  },
  'np.zeros': {
    sig: 'np.zeros(shape)',
    desc: '创建指定形状的全 0 数组。',
    op: '按形状分配数组并把元素全填 0。',
    inputs: 'shape；可选 dtype。',
    outputs: '全 0 数组。',
  },
  'np.empty': {
    sig: 'np.empty(shape)',
    desc: '创建未初始化数组（内容是垃圾值）。',
    op: '只分配内存不清零，需自己写入。',
    inputs: 'shape；可选 dtype。',
    outputs: '内容未定义的数组。',
  },
  'np.arange': {
    sig: 'np.arange(start, stop, step)',
    desc: '生成等差序列（不含 stop）。',
    op: '从 start 每次加 step 直到 stop 前，生成一维数组。',
    inputs: 'start、stop、step。',
    outputs: '一维数组。',
  },
  'np.matmul': {
    sig: 'np.matmul(a, b)',
    desc: '矩阵乘法（支持 batch），等价于 a @ b。',
    op: '对最后两维做矩阵乘，前面 batch 维按广播对齐。',
    inputs: 'a、b 数组（内维匹配）。',
    outputs: '矩阵乘结果。',
  },
  'np.dot': {
    sig: 'np.dot(a, b)',
    desc: '点积 / 矩阵乘（一维时是内积，二维时是矩阵乘）。',
    op: '一维得标量内积；二维等价矩阵乘。',
    inputs: 'a、b 数组。',
    outputs: '标量或数组。',
  },
  'np.transpose': {
    sig: 'np.transpose(x, axes)',
    desc: '按给定顺序重排数组的轴（默认整体转置）。',
    op: '重新排列维度顺序，只改视图不复制数据。',
    inputs: 'x 数组；axes 轴的新顺序（可选）。',
    outputs: '轴重排后的视图。',
  },
  'np.swapaxes': {
    sig: 'np.swapaxes(x, a, b)',
    desc: '交换两个指定的轴。',
    op: '把第 a 轴和第 b 轴对调，只改视图。',
    inputs: 'x 数组；a、b 轴编号。',
    outputs: '交换轴后的视图。',
  },
  'np.argsort': {
    sig: 'np.argsort(x, axis)',
    desc: '返回把数组排序所需的索引。',
    op: '沿 axis 排序，返回能让原数组升序排列的下标。',
    inputs: 'x 数组；axis。',
    outputs: '索引数组。',
  },
  'np.argpartition': {
    sig: 'np.argpartition(x, k)',
    desc: '部分排序：把第 k 小的元素放到正确位置，比全排序快。',
    op: '只保证第 k 位就位、左边更小右边更大，用于取 top-k。',
    inputs: 'x 数组；k 分界位置。',
    outputs: '索引数组。',
  },
  'np.outer': {
    sig: 'np.outer(a, b)',
    desc: '外积：一维 a、b 生成矩阵 R[i,j] = a[i]·b[j]。',
    op: '把 a 当列、b 当行相乘，得到 (len(a), len(b)) 矩阵。',
    inputs: 'a、b 一维数组。',
    outputs: '二维外积矩阵。',
  },
  'np.expand_dims': {
    sig: 'np.expand_dims(x, axis)',
    desc: '在指定位置插入一个长度为 1 的新维度。',
    op: '给数组加一维（方便广播），等价于索引里的 None。',
    inputs: 'x 数组；axis 插入位置。',
    outputs: '多一维的数组。',
  },
  'np.convolve': {
    sig: 'np.convolve(a, v, mode)',
    desc: '一维卷积（会翻转 kernel，和互相关不同）。',
    op: '滑动加权求和，mode 控制输出长度（full/same/valid）。',
    inputs: 'a、v 一维数组；mode。',
    outputs: '卷积结果一维数组。',
  },
  'np.exp': {
    sig: 'np.exp(x)',
    desc: '逐元素求自然指数 eˣ。',
    op: '对每个元素算 e 的该值次幂。',
    inputs: 'x 数组。',
    outputs: '同形数组。',
  },
  'np.abs': {
    sig: 'np.abs(x)',
    desc: '逐元素求绝对值。',
    op: '每个元素取绝对值。',
    inputs: 'x 数组。',
    outputs: '同形数组。',
  },
  'np.reshape': {
    sig: 'np.reshape(x, shape)',
    desc: '在不改数据的前提下改变数组形状。',
    op: '按新 shape 重新解释同一块数据；某维写 -1 让 numpy 自动推断。',
    inputs: 'x 数组；shape（元素总数须一致）。',
    outputs: '新形状的视图。',
  },
  'F.binary_cross_entropy': {
    sig: 'F.binary_cross_entropy(probs, target)',
    desc: '二分类交叉熵，输入是概率（已过 sigmoid，非 logits）。',
    op: '算 -[y·log p + (1-y)·log(1-p)] 并平均；不如 with_logits 稳定。',
    inputs: 'probs ∈ (0,1) 与 target 同形。',
    outputs: '标量损失。',
  },
  'np.ascontiguousarray': {
    sig: 'np.ascontiguousarray(x)',
    desc: '返回内存连续（C-order）的数组副本。',
    op: '若 x 内存不连续（如切片/转置后的视图）就复制成连续布局，否则原样返回；某些操作要求连续内存。',
    inputs: 'x 数组。',
    outputs: '内存连续的数组。',
  },
  'np.result_type': {
    sig: 'np.result_type(a, b, ...)',
    desc: '推断多个输入运算后会得到的公共 dtype。',
    op: '按 numpy 的类型提升规则，算出这些输入一起运算时的结果类型（如 int + float → float）。',
    inputs: '若干数组 / dtype / 标量。',
    outputs: '一个 dtype。',
  },
  'np.newaxis': {
    sig: 'x[:, np.newaxis]',
    desc: '在索引里插入一个长度为 1 的新维度（其实就是 None）。',
    op: '放在索引位置处给数组加一维，常用来制造广播（如把 (N,) 变 (N,1)）。',
    inputs: '用于索引，非函数调用。',
    outputs: '多一维的视图。',
  },
  'np.empty_like': {
    sig: 'np.empty_like(x)',
    desc: '创建和 x 同形状/同 dtype 的未初始化数组。',
    op: '只分配内存不清零，需自己写入；比 zeros_like 略快。',
    inputs: 'x 数组。',
    outputs: '与 x 同形、内容未定义的数组。',
  },
}
